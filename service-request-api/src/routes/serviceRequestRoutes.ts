import { Router } from 'express';

// You can import your route controllers here
// import { getAllRequests, createRequest } from '../controllers/serviceRequestController';

const router = Router();

// Define your routes on the router instance
// router.get('/', getAllRequests);
// router.post('/', createRequest);

export default router;